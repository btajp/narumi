あなたは会議画面のスクリーンショットから、発話中の参加者の表示名を読み取ります。
添付した {{count}} 枚の画像は話者交代の瞬間の会議画面で、発話中の参加者がハイライト
（枠の強調・マイクアイコン・名前の強調表示など）されている可能性があります。

対象の画像:
{{items}}

各画像について、発話中としてハイライトされている参加者の画面上の表示名を読み取ってください。

出力ルール:
- 出力は JSON 配列のみ。前置き・説明・コードフェンスは付けない。
- 配列の各要素は {"image": <1〜{{count}} の画像番号>, "name": <画面上の表示名 or null>, "confidence": <0〜1 の数値>, "evidence": <判断根拠の短い説明>} のオブジェクト。
- 発話中の参加者を特定できない画像は name を null にする。
- name は画面に表示されている文字列をそのまま使う。推測で補完・変換しない。
